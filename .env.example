# The administrator this site is edited as. The image refuses to start without it.
ASSEMORA_SEED_PASSWORD=

# Where the content lives. Unset, the site runs in memory and every restart reseeds —
# which serves the site correctly but means an edit made in Studio does not survive one.
DATABASE_URL=
